# 📘 Пошаговая инструкция: Настройка VPS для booking-app

## 🎯 Что мы делаем
Настраиваем собственный сервер (VPS) с Ubuntu, чтобы запустить проект полностью анонимно и автоматически.

---

## ШАГ 1: Регистрация на VPS провайдере

### Вариант 1: Hetzner Cloud (рекомендую - дешево)
1. Открой: https://www.hetzner.com/cloud
2. Нажми **"Sign up"** (Регистрация)
3. Заполни форму:
   - Email: можно использовать временный (10minutemail.com, tempmail.org)
   - Пароль: придумай надежный
   - Страна: любая
4. Подтверди email (проверь временный почтовый ящик)
5. Войди в аккаунт

### Вариант 2: DigitalOcean
1. Открой: https://www.digitalocean.com/
2. Нажми **"Sign up"**
3. Заполни форму (можно использовать временный email)
4. Подтверди email

### Вариант 3: Vultr
1. Открой: https://www.vultr.com/
2. Нажми **"Sign Up"**
3. Заполни форму

---

## ШАГ 2: Создание сервера (VPS)

### На Hetzner Cloud:
1. Войди в панель управления
2. Нажми **"Add Server"** (Добавить сервер)
3. Настройки:
   - **Location**: выбери ближайший к твоим пользователям (например, Germany, Finland)
   - **Image**: выбери **Ubuntu 22.04**
   - **Type**: выбери **CX11** (2GB RAM, 1 vCPU) - ~€4/месяц
     - Или **CPX11** (2GB RAM, 2 vCPU) - ~€5/месяц (рекомендую)
   - **SSH Keys**: можно пропустить (будем использовать пароль)
   - **Networks**: оставь по умолчанию
   - **Firewalls**: можно пропустить
   - **Backups**: можно отключить (экономия)
   - **Cloud Config**: оставь пустым
4. Нажми **"Create & Buy Now"**
5. **ВАЖНО**: Сохрани пароль root! Он покажется один раз.

### На DigitalOcean:
1. Нажми **"Create"** → **"Droplets"**
2. Настройки:
   - **Choose an image**: Ubuntu 22.04 (LTS)
   - **Choose a plan**: Basic → **Regular** (1GB RAM) - $6/месяц
   - **Choose a datacenter region**: ближайший
   - **Authentication**: **Password** (проще) или SSH keys
3. Нажми **"Create Droplet"**
4. Сохрани пароль root (придет на email)

### На Vultr:
1. Нажми **"Deploy Server"**
2. Настройки:
   - **Server Type**: Cloud Compute
   - **CPU & Storage**: Regular Performance → **$6/mo** (1GB RAM)
   - **Server Location**: ближайший
   - **Operating System**: Ubuntu 22.04 LTS
   - **SSH Keys**: можно пропустить
3. Нажми **"Deploy Now"**
4. Сохрани пароль root

---

## ШАГ 3: Подключение к серверу

### Через терминал (Mac/Linux):
1. Открой терминал
2. Выполни команду:
   ```bash
   ssh root@ТВОЙ_IP_АДРЕС
   ```
   Например: `ssh root@123.45.67.89`
3. Введи пароль (который сохранил на шаге 2)
4. При первом подключении спросит "Are you sure you want to continue connecting?" - введи **yes**

### Через Windows (PuTTY):
1. Скачай PuTTY: https://www.putty.org/
2. Установи и открой
3. В поле **Host Name**: введи IP адрес сервера
4. **Port**: 22
5. Нажми **Open**
6. Введи логин: **root**
7. Введи пароль (который сохранил)

### Если не получается подключиться:
- Проверь, что IP адрес правильный
- Проверь, что пароль скопирован полностью (без пробелов)
- Попробуй через веб-консоль провайдера (обычно есть кнопка "Console" или "Web Console")

---

## ШАГ 4: Автоматическая настройка сервера

После подключения к серверу выполни **ОДНУ команду**:

```bash
curl -fsSL https://raw.githubusercontent.com/Dimeeee1488/booking-app/main/scripts/setup-server.sh | bash
```

Эта команда автоматически:
- ✅ Обновит систему
- ✅ Установит Node.js 20.x
- ✅ Установит PM2 (для управления процессом)
- ✅ Установит Nginx (веб-сервер)
- ✅ Установит Certbot (для SSL)
- ✅ Установит Git
- ✅ Настроит файрвол

**Жди завершения** (обычно 2-5 минут). В конце увидишь сообщение "✅ Настройка завершена!"

---

## ШАГ 5: Клонирование проекта

Выполни команды:

```bash
cd ~
git clone https://github.com/Dimeeee1488/booking-app.git
cd booking-app
npm install
```

Жди завершения установки зависимостей (1-2 минуты).

---

## ШАГ 6: Настройка переменных окружения

Создай файл с настройками:

```bash
nano .env
```

Откроется редактор. Вставь следующее (замени значения на свои):

```env
PORT=3001
NODE_ENV=production

# RapidAPI ключ (получи на https://rapidapi.com/)
RAPIDAPI_KEY=твой_rapidapi_ключ_здесь
RAPIDAPI_HOST=booking-com15.p.rapidapi.com

# Telegram Bot (опционально, если используешь)
TELEGRAM_BOT_TOKEN=твой_telegram_bot_token
TELEGRAM_CHAT_ID=твой_telegram_chat_id

# CORS (можно оставить * для начала)
CORS_ORIGIN=*
```

**Как редактировать в nano:**
- Вставляй текст: `Ctrl+Shift+V` (Mac) или `Shift+Insert` (Windows)
- Сохранить и выйти: `Ctrl+X`, затем `Y`, затем `Enter`

**Где взять ключи:**
- **RAPIDAPI_KEY**: https://rapidapi.com/ → зарегистрируйся → найди API "Booking.com" → скопируй ключ
- **TELEGRAM_BOT_TOKEN**: создай бота через @BotFather в Telegram
- **TELEGRAM_CHAT_ID**: получи через @userinfobot в Telegram

---

## ШАГ 7: Сборка проекта

```bash
npm run build
```

Жди завершения (30-60 секунд). В конце должно быть "build completed successfully".

---

## ШАГ 8: Запуск приложения

```bash
pm2 start server.cjs --name booking-app
pm2 save
pm2 startup
```

**Важно**: После `pm2 startup` появится команда вида:
```
sudo env PATH=... pm2 startup systemd -u root --hp /root
```

**Скопируй и выполни эту команду** (она нужна для автозапуска при перезагрузке).

Проверь статус:
```bash
pm2 status
```

Должно показать `booking-app` со статусом `online`.

---

## ШАГ 9: Настройка Nginx (веб-сервер)

Создай конфигурацию:

```bash
nano /etc/nginx/sites-available/booking-app
```

Вставь следующее:

```nginx
server {
    listen 80;
    server_name _;

    access_log /var/log/nginx/booking-app-access.log;
    error_log /var/log/nginx/booking-app-error.log;

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}
```

Сохрани: `Ctrl+X`, затем `Y`, затем `Enter`

Активируй конфигурацию:

```bash
rm -f /etc/nginx/sites-enabled/default
ln -s /etc/nginx/sites-available/booking-app /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

Если `nginx -t` показал "syntax is ok", значит всё правильно!

---

## ШАГ 10: Проверка работы

Открой в браузере:
```
http://ТВОЙ_IP_АДРЕС
```

Например: `http://123.45.67.89`

**Должен открыться сайт!** 🎉

Если не открывается:
1. Проверь статус: `pm2 status` (должно быть `online`)
2. Проверь логи: `pm2 logs booking-app`
3. Проверь Nginx: `systemctl status nginx`

---

## ШАГ 11: Настройка SSL (HTTPS) - опционально

### Вариант A: С доменом

Если у тебя есть домен:
1. Настрой DNS записи у регистратора:
   - **A запись**: `@` → `ТВОЙ_IP_АДРЕС`
   - **A запись**: `www` → `ТВОЙ_IP_АДРЕС`
2. Подожди 10-30 минут (распространение DNS)
3. На сервере выполни:
   ```bash
   certbot --nginx -d твойдомен.com -d www.твойдомен.com
   ```
4. Следуй инструкциям (введи email, согласись с условиями)
5. Готово! Сайт будет доступен по `https://твойдомен.com`

### Вариант B: Без домена (только по IP)

Можно использовать самоподписанный сертификат:

```bash
openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout /etc/ssl/private/booking-app.key \
  -out /etc/ssl/certs/booking-app.crt \
  -subj "/C=US/ST=State/L=City/O=Org/CN=localhost"
```

Обнови конфигурацию Nginx:

```bash
nano /etc/nginx/sites-available/booking-app
```

Добавь SSL блок:

```nginx
server {
    listen 443 ssl;
    server_name _;

    ssl_certificate /etc/ssl/certs/booking-app.crt;
    ssl_certificate_key /etc/ssl/private/booking-app.key;

    access_log /var/log/nginx/booking-app-access.log;
    error_log /var/log/nginx/booking-app-error.log;

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

server {
    listen 80;
    server_name _;
    return 301 https://$server_name$request_uri;
}
```

Перезапусти:
```bash
nginx -t
systemctl restart nginx
```

**Примечание**: Браузер покажет предупреждение о самоподписанном сертификате - это нормально, нажми "Продолжить".

---

## ШАГ 12: Автоматическое обновление (опционально)

Настрой автоматическое обновление из GitHub:

```bash
nano ~/booking-app/auto-update.sh
```

Вставь:

```bash
#!/bin/bash
cd ~/booking-app
git pull origin main
npm install
npm run build
pm2 restart booking-app
```

Сохрани и сделай исполняемым:

```bash
chmod +x ~/booking-app/auto-update.sh
```

Добавь в cron (обновление каждый день в 3:00):

```bash
crontab -e
```

Добавь строку:
```
0 3 * * * /root/booking-app/auto-update.sh >> /var/log/booking-app-update.log 2>&1
```

Сохрани: `Ctrl+X`, затем `Y`, затем `Enter`

---

## ✅ Готово!

Теперь у тебя:
- ✅ Собственный сервер
- ✅ Приложение работает автоматически
- ✅ Автозапуск при перезагрузке
- ✅ Автоматическое обновление (если настроил)
- ✅ Полный контроль

---

## 🔧 Полезные команды

### Управление приложением:
```bash
pm2 status              # Статус
pm2 restart booking-app # Перезапуск
pm2 stop booking-app    # Остановка
pm2 logs booking-app    # Логи
```

### Ручное обновление:
```bash
cd ~/booking-app
git pull origin main
npm install
npm run build
pm2 restart booking-app
```

### Проверка Nginx:
```bash
systemctl status nginx  # Статус
systemctl restart nginx # Перезапуск
nginx -t                # Проверка конфигурации
```

### Просмотр логов:
```bash
pm2 logs booking-app                    # Логи приложения
tail -f /var/log/nginx/booking-app-access.log  # Логи Nginx (доступ)
tail -f /var/log/nginx/booking-app-error.log    # Логи Nginx (ошибки)
```

---

## 🛡️ Безопасность (рекомендуется)

### Отключение root логина (опционально):
```bash
# Создай нового пользователя
adduser deploy
usermod -aG sudo deploy

# Настрой SSH ключ для нового пользователя
su - deploy
mkdir -p ~/.ssh
nano ~/.ssh/authorized_keys
# Вставь свой публичный SSH ключ

# Отключи root логин
sudo nano /etc/ssh/sshd_config
# Найди строку: PermitRootLogin yes
# Измени на: PermitRootLogin no
sudo systemctl restart sshd
```

---

## 💰 Стоимость

- **VPS**: ~$5-10/месяц (Hetzner самый дешевый - €4-5/месяц)
- **Домен**: опционально, ~$10-15/год
- **Итого**: ~$60-120/год (или меньше без домена)

---

## ❓ Проблемы?

### Приложение не запускается:
```bash
pm2 logs booking-app  # Посмотри логи
cd ~/booking-app
npm run build         # Пересобери проект
pm2 restart booking-app
```

### Nginx не работает:
```bash
systemctl status nginx
nginx -t              # Проверь конфигурацию
tail -f /var/log/nginx/error.log
```

### Не могу подключиться по SSH:
- Проверь IP адрес
- Проверь пароль (скопируй полностью, без пробелов)
- Попробуй через веб-консоль провайдера

---

## 🎉 Всё готово!

Теперь у тебя полностью рабочий сервер с автоматическим управлением. Ничего проверять не нужно - всё работает само!

