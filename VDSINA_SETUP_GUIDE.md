# 🚀 Пошаговая инструкция: VDSina.com (Российский провайдер)

## 🎯 Что мы делаем
Настраиваем VPS на VDSina.com - российский провайдер с русской поддержкой.

---

## ✅ Преимущества VDSina.com

- ✅ **Очень дешево**: от 100-200₽/месяц (~$1-2/месяц)
- ✅ **Русская поддержка**: можно общаться на русском
- ✅ **Русский интерфейс**: всё понятно
- ✅ **Быстрая регистрация**: можно начать сразу
- ✅ **Разные способы оплаты**: карты, электронные кошельки
- ✅ **Работает в России**: без проблем с доступом

---

## ШАГ 1: Регистрация на VDSina.com

1. Открой: https://www.vdsina.com
2. Нажми **"Регистрация"** или **"Sign up"** (обычно в правом верхнем углу)
3. Заполни форму:
   - **Email**: можно использовать временный (10minutemail.com, tempmail.org)
   - **Пароль**: придумай надежный пароль
   - **Телефон**: можно использовать временный номер (если требуется)
4. Нажми **"Зарегистрироваться"** или **"Sign up"**
5. **Подтверди email:**
   - Проверь почтовый ящик
   - Найди письмо от VDSina
   - Нажми на ссылку подтверждения
6. Войди в аккаунт

**Примечание:** Для анонимности можно использовать временный email.

---

## ШАГ 2: Пополнение баланса

1. Войди в панель управления
2. Перейди в **"Пополнить баланс"** или **"Add Funds"**
3. Выбери способ оплаты:
   - **Банковская карта** (Visa, MasterCard)
   - **Электронные кошельки** (Qiwi, Яндекс.Деньги, WebMoney)
   - **Криптовалюта** (если доступно)
4. Выбери сумму (минимум обычно 100-200₽):
   - Для начала: 200-300₽ (хватит на 1-2 месяца)
   - Рекомендую: 500-1000₽ (хватит на 3-6 месяцев)
5. Оплати
6. Баланс пополнится автоматически (обычно сразу)

---

## ШАГ 3: Создание сервера (VDS)

1. В панели управления нажми **"Создать VDS"** или **"Заказать сервер"**
2. Настройки сервера:

   **ОС (Операционная система):**
   - Выбери **Ubuntu 22.04 LTS** (рекомендую)
   - Или **Ubuntu 20.04 LTS** (тоже подойдет)

   **Тариф:**
   - **VDS Start** или **Basic**: 1GB RAM, 1 CPU, 10-20GB диск - от 100-150₽/месяц (для начала)
   - **VDS Standard**: 2GB RAM, 2 CPU, 20-40GB диск - от 200-300₽/месяц (рекомендую)
   - **VDS Pro**: 4GB RAM, 4 CPU, 40-80GB диск - от 400-500₽/месяц (если нужна большая мощность)

   **Локация:**
   - Выбери ближайшую (Москва, Санкт-Петербург, или другая)

   **Диск:**
   - Минимум 10-20GB (обычно включено в базовый тариф)

   **Пароль root:**
   - Придумай надежный пароль
   - Или используй генератор паролей
   - **ВАЖНО**: Сохрани пароль!

   **SSH ключ:**
   - Можно пропустить (будем использовать пароль)
   - Или добавь свой SSH ключ для большей безопасности

3. Нажми **"Создать"** или **"Заказать"**
4. Подожди 5-10 минут, пока сервер создается
5. После создания увидишь:
   - **IP адрес сервера** (например: 123.45.67.89)
   - **Статус**: "Активен" или "Running"

**Сохрани:**
- IP адрес сервера
- Пароль root

---

## ШАГ 4: Подключение к серверу

### Через терминал (Mac/Linux):

1. Открой терминал
2. Выполни команду:
   ```bash
   ssh root@ТВОЙ_IP_АДРЕС
   ```
   Например: `ssh root@123.45.67.89`
3. При первом подключении спросит:
   ```
   The authenticity of host '123.45.67.89' can't be established.
   Are you sure you want to continue connecting (yes/no)?
   ```
   Введи: **yes**
4. Введи пароль root (который сохранил на шаге 3)
5. Готово! Ты подключен к серверу

### Через Windows (PuTTY):

1. Скачай PuTTY: https://www.putty.org/
2. Установи и открой
3. В поле **Host Name**: введи IP адрес сервера
4. **Port**: 22
5. Нажми **Open**
6. Введи логин: **root**
7. Введи пароль (который сохранил)

### Если не получается подключиться:

- Проверь IP адрес (должен быть правильный)
- Проверь пароль (скопируй полностью, без пробелов)
- Попробуй через веб-консоль VDSina:
  - В панели управления → выбери сервер → **"Консоль"** или **"Console"** → откроется веб-консоль

---

## ШАГ 5: Автоматическая настройка сервера

После подключения к серверу выполни **ОДНУ команду**:

```bash
curl -fsSL https://raw.githubusercontent.com/Dimeeee1488/booking-app/main/scripts/setup-server.sh | bash
```

Эта команда автоматически:
- ✅ Обновит систему
- ✅ Установит Node.js 20.x
- ✅ Установит PM2 (для управления процессом)
- ✅ Установит Nginx (веб-сервер)
- ✅ Установит Certbot (для SSL)
- ✅ Установит Git
- ✅ Настроит файрвол

**Жди завершения** (обычно 2-5 минут). В конце увидишь:
```
✅ Настройка завершена!
```

---

## ШАГ 6: Клонирование проекта

Выполни команды:

```bash
cd ~
git clone https://github.com/Dimeeee1488/booking-app.git
cd booking-app
npm install
```

Жди завершения установки зависимостей (1-2 минуты).

---

## ШАГ 7: Настройка переменных окружения

Создай файл с настройками:

```bash
nano .env
```

Откроется редактор. Вставь следующее (замени значения на свои):

```env
PORT=3001
NODE_ENV=production

# RapidAPI ключ (получи на https://rapidapi.com/)
RAPIDAPI_KEY=твой_rapidapi_ключ_здесь
RAPIDAPI_HOST=booking-com15.p.rapidapi.com

# Telegram Bot (опционально, если используешь)
TELEGRAM_BOT_TOKEN=твой_telegram_bot_token
TELEGRAM_CHAT_ID=твой_telegram_chat_id

# CORS (можно оставить * для начала)
CORS_ORIGIN=*
```

**Как редактировать в nano:**
- Вставляй текст: `Ctrl+Shift+V` (Mac) или `Shift+Insert` (Windows)
- Сохранить и выйти: `Ctrl+X`, затем `Y`, затем `Enter`

**Где взять ключи:**
- **RAPIDAPI_KEY**: https://rapidapi.com/ → зарегистрируйся → найди API "Booking.com" → скопируй ключ
- **TELEGRAM_BOT_TOKEN**: создай бота через @BotFather в Telegram
- **TELEGRAM_CHAT_ID**: получи через @userinfobot в Telegram

---

## ШАГ 8: Сборка проекта

```bash
npm run build
```

Жди завершения (30-60 секунд). В конце должно быть "build completed successfully".

---

## ШАГ 9: Запуск приложения

```bash
pm2 start server.cjs --name booking-app
pm2 save
pm2 startup
```

**Важно**: После `pm2 startup` появится команда вида:
```
sudo env PATH=$PATH:/usr/bin pm2 startup systemd -u root --hp /root
```

**Скопируй и выполни эту команду** (она нужна для автозапуска при перезагрузке).

Проверь статус:
```bash
pm2 status
```

Должно показать `booking-app` со статусом `online` (зеленый).

---

## ШАГ 10: Настройка Nginx (веб-сервер)

Создай конфигурацию:

```bash
nano /etc/nginx/sites-available/booking-app
```

Вставь следующее:

```nginx
server {
    listen 80;
    server_name _;

    access_log /var/log/nginx/booking-app-access.log;
    error_log /var/log/nginx/booking-app-error.log;

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}
```

Сохрани: `Ctrl+X`, затем `Y`, затем `Enter`

Активируй конфигурацию:

```bash
rm -f /etc/nginx/sites-enabled/default
ln -s /etc/nginx/sites-available/booking-app /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

Если `nginx -t` показал "syntax is ok", значит всё правильно!

---

## ШАГ 11: Проверка работы

Открой в браузере:
```
http://ТВОЙ_IP_АДРЕС
```

Например: `http://123.45.67.89`

**Должен открыться сайт!** 🎉

Если не открывается:
1. Проверь статус: `pm2 status` (должно быть `online`)
2. Проверь логи: `pm2 logs booking-app`
3. Проверь Nginx: `systemctl status nginx`

---

## ШАГ 12: Настройка SSL (HTTPS) - опционально

### Вариант A: С доменом

Если у тебя есть домен:
1. Настрой DNS записи у регистратора:
   - **A запись**: `@` → `ТВОЙ_IP_АДРЕС`
   - **A запись**: `www` → `ТВОЙ_IP_АДРЕС`
2. Подожди 10-30 минут (распространение DNS)
3. На сервере выполни:
   ```bash
   certbot --nginx -d твойдомен.com -d www.твойдомен.com
   ```
4. Следуй инструкциям (введи email, согласись с условиями)
5. Готово! Сайт будет доступен по `https://твойдомен.com`

### Вариант B: Без домена (только по IP)

Можно использовать самоподписанный сертификат:

```bash
openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout /etc/ssl/private/booking-app.key \
  -out /etc/ssl/certs/booking-app.crt \
  -subj "/C=RU/ST=Moscow/L=Moscow/O=Org/CN=localhost"
```

Обнови конфигурацию Nginx:

```bash
nano /etc/nginx/sites-available/booking-app
```

Добавь SSL блок:

```nginx
server {
    listen 443 ssl;
    server_name _;

    ssl_certificate /etc/ssl/certs/booking-app.crt;
    ssl_certificate_key /etc/ssl/private/booking-app.key;

    access_log /var/log/nginx/booking-app-access.log;
    error_log /var/log/nginx/booking-app-error.log;

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

server {
    listen 80;
    server_name _;
    return 301 https://$server_name$request_uri;
}
```

Перезапусти:
```bash
nginx -t
systemctl restart nginx
```

**Примечание**: Браузер покажет предупреждение о самоподписанном сертификате - это нормально, нажми "Продолжить".

---

## ШАГ 13: Автоматическое обновление (опционально)

Настрой автоматическое обновление из GitHub:

```bash
nano ~/booking-app/auto-update.sh
```

Вставь:

```bash
#!/bin/bash
cd ~/booking-app
git pull origin main
npm install
npm run build
pm2 restart booking-app
```

Сохрани и сделай исполняемым:

```bash
chmod +x ~/booking-app/auto-update.sh
```

Добавь в cron (обновление каждый день в 3:00):

```bash
crontab -e
```

Добавь строку:
```
0 3 * * * /root/booking-app/auto-update.sh >> /var/log/booking-app-update.log 2>&1
```

Сохрани: `Ctrl+X`, затем `Y`, затем `Enter`

---

## ✅ Готово!

Теперь у тебя:
- ✅ Собственный сервер на VDSina.com
- ✅ Русская поддержка
- ✅ Приложение работает автоматически
- ✅ Автозапуск при перезагрузке
- ✅ Автоматическое обновление (если настроил)

---

## 🔧 Полезные команды

### Управление приложением:
```bash
pm2 status              # Статус
pm2 restart booking-app # Перезапуск
pm2 stop booking-app    # Остановка
pm2 logs booking-app    # Логи
```

### Ручное обновление:
```bash
cd ~/booking-app
git pull origin main
npm install
npm run build
pm2 restart booking-app
```

### Проверка Nginx:
```bash
systemctl status nginx  # Статус
systemctl restart nginx # Перезапуск
nginx -t                # Проверка конфигурации
```

### Просмотр логов:
```bash
pm2 logs booking-app                           # Логи приложения
tail -f /var/log/nginx/booking-app-access.log # Логи Nginx (доступ)
tail -f /var/log/nginx/booking-app-error.log  # Логи Nginx (ошибки)
```

---

## 💰 Стоимость

- **VPS**: от 100-200₽/месяц (~$1-2/месяц) - самый дешевый вариант
- **Домен**: опционально, ~$10-15/год
- **Итого**: ~$12-40/год (~1200-4000₽/год) или меньше без домена

**Оплата:**
- ✅ Банковская карта
- ✅ Электронные кошельки (Qiwi, Яндекс.Деньги, WebMoney)
- ✅ Криптовалюта (если доступно)

---

## 🛡️ Безопасность (рекомендуется)

### Настройка файрвола:
```bash
ufw status              # Проверка статуса
ufw allow 22/tcp       # SSH
ufw allow 80/tcp       # HTTP
ufw allow 443/tcp      # HTTPS
ufw enable             # Включить файрвол
```

### Отключение root логина (опционально):
```bash
# Создай нового пользователя
adduser deploy
usermod -aG sudo deploy

# Настрой SSH ключ для нового пользователя
su - deploy
mkdir -p ~/.ssh
nano ~/.ssh/authorized_keys
# Вставь свой публичный SSH ключ

# Отключи root логин
sudo nano /etc/ssh/sshd_config
# Найди строку: PermitRootLogin yes
# Измени на: PermitRootLogin no
sudo systemctl restart sshd
```

---

## ❓ Проблемы?

### Приложение не запускается:
```bash
pm2 logs booking-app  # Посмотри логи
cd ~/booking-app
npm run build         # Пересобери проект
pm2 restart booking-app
```

### Nginx не работает:
```bash
systemctl status nginx
nginx -t              # Проверь конфигурацию
tail -f /var/log/nginx/error.log
```

### Не могу подключиться по SSH:
- Проверь IP адрес
- Проверь пароль (скопируй полностью, без пробелов)
- Попробуй через веб-консоль VDSina (в панели управления → Консоль)

### Проблемы с оплатой:
- Свяжись с поддержкой VDSina (русская поддержка)
- Проверь баланс в панели управления
- Попробуй другой способ оплаты

---

## 🎉 Всё готово!

Теперь у тебя полностью рабочий сервер на VDSina.com с русской поддержкой. Ничего проверять не нужно - всё работает автоматически!

**Преимущества:**
- ✅ Дешево (от 100₽/месяц)
- ✅ Русская поддержка
- ✅ Работает в России
- ✅ Автоматическое управление

Удачи! 🚀

